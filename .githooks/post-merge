#!/usr/bin/env bash
set -euo pipefail

echo "=== post-merge: re-stowing packages ==="
script="$(git rev-parse --show-toplevel)/scripts/stow.sh"
if [[ -x "$script" ]]; then
  "$script"
else
  echo "  WARN: stow.sh not found or not executable"
fi
